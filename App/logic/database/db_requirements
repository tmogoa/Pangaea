This file will have the require tables for the database.
Abart please check this file regularly and update the database using the migration.php

All ids should be unsigned int.

Tables needed
1. users table
   -userId, firstname, lastname, phone, email, password

2. Article table
   -articleId, writerId (foreign key referencing the userId), articleText, publishStatus(published, draft), created_at, updated_at

3. ArticleReaction table
   -articleId (foreign key), claps, clappers(This should be a varchar), shares (int unsigned)

4. Reading table
    -readingId, readerId (userId), articleId, timeReading

